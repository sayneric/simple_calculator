#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdarg.h>

void display_menu(void);
void operations(void);
int add(int count, ...);
int sub(int sub, ...);

#endif
